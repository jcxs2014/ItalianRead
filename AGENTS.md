# Rule: ItalianRead 精读会话工作流

## 工作区信息
- 本工作区：`~/Documents/Works/ItalianRead/`
- 精读保存根目录：本工作区自身（所有 md 文件保存于此）
- **重要目录**：
  - `storybook/` — 故事 epub 源文件（不 git 追踪）
  - `news/` — 新闻 epub 源文件（不 git 追踪，Calibre 抓取）
  - `content/racconti/` — 故事精读文档
  - `content/doppiozero/` — 文章精读文档

## 精读会话流程
1. 每次收到意大利语文本后，**只在会话中输出简短的状态/标题/简短结构说明**，不输出大段精读内容
2. **完整的精读详情（词汇、语法、写作技巧、句型分析等）直接写入 md 文件**
3. 文件命名格式：
   - **文章**：`标题_snake_case.md`
   - **故事**：`编号_标题_snake_case.md`（编号根据来源总篇数决定：<100篇用2位如`01`，≥100篇用3位如`001`）
4. 保存路径：
   - **文章**：`~/Documents/Works/ItalianRead/content/<source>/<日期_星期>/`
   - **故事**：`~/Documents/Works/ItalianRead/content/racconti/<source>/`
5. 文件名基于标题自动生成（用下划线代替空格，去掉标点）
6. 每次会话开头不再需要等"继续"指令；用户给文本就分析+保存

## 文章精读格式（A1-C1）

### A1/A2 级别精读重点

> **针对初级（A1/A2）学习者的特殊处理**

#### 动词变位标注
每个原句分析必须包含**动词变位解析**：
- 标注主语人称 + 动词形式
- 解释变位规律（-are/-ere/-ire 三大变位法）
- 指出不规则变位

#### 名词单复数
- 标注名词单复数形式
- 解释名词性数配合规则

#### 前置词搭配
- 标注动词+前置词、名词+前置词的固定搭配
- 解释常见前置词（a, di, da, per, con, su, in, tra/fra）的用法

#### 语法解释深度
- A1/A2 文章：增加**基础语法解释**子项
- B1+ 文章：侧重**高级表达**和**长难句结构**

### 文章精读文件格式

文件层级固定为：
0. `frontmatter`：`---` / `lang: it` / `状态: 未读` / `难度: A1|A2|B1|B2` / `---`
1. `# 标题（精读分析）`
2. `## 概览`：来源/栏目/副题 → 主题一段话 → 结构列表 → 段落脉络表格 → 核心金句
3. `## 逐句精读`：每个原文段落一个 `### 第 N 段：主题概括`；段内每个原句一个 `> **原句 M:**` 分析块，固定含**中文理解 / 句子结构 / 动词变位 / 关键词 / 表达方式 / 为什么这样写**六子项（A1/A2 文章增加"基础语法解释"）；每段末尾 `**段落逻辑：**` 用 → 箭头串起该段逻辑链；原句编号 M 全文连续不重置
4. `## 段落逻辑`（可选，全文级逻辑梳理）
5. `## 词汇分级`（独立章节，含四档子标题）
   - `### ⭐ 基础（A1-A2）`（词/短语 | 释义 | 难度 | 例句 表格）
   - `### ⭐⭐ 中级（B1）`
   - `### ⭐⭐⭐ 进阶（B2）`
   - `### ⭐⭐⭐⭐ 高级（C1）`
6. `## 长难句专项`（2-5 个长难句逐句解剖）
7. `## 精读结束总结`（含子标题：1. 本文核心表达 / 2. 重要语法 / 3. 写作技巧等）
8. `## 可迁移表达`（独立章节）

**顺序核心规范**：5 → 6 → 7 → 8（词汇分级 → 长难句专项 → 精读结束总结 → 可迁移表达），四章顺序不得打乱。
**核心规范**：每个原句一个分析块，禁止多句合并成一个分析块；结尾无 ■ 标记。

## 故事精读格式（B1-C1）

> **故事精读与文章精读的核心区别**：故事以情节推进为单位（段落/章节），而非逐句分析；需关注人物、叙事手法、主题等文学维度。

### 故事精读文件格式

文件层级固定为：
0. `frontmatter`：`---` / `lang: it` / `状态: 未读` / `难度: B1|B2|C1` / `source: <来源>` / `author: <作者>` / `---`
1. `# 标题`
2. `## 概要`
   - **情节摘要**：一段话概括（2-3句）
   - **时间/地点**：故事发生的时空背景
   - **主要人物**：人物列表及一句话描述
3. `## 原文与解析`
   - 按故事自然段落分chunk，每chunk结构：
     - `### 第N段：段落主题`
     - `**原文**`：精选段落原文
     - `**翻译**`：自然中文翻译
     - `**注释**`：词汇/语法/写作手法/文化背景（选二重点，不求全）
   - 原文分段3-6段为宜，每段2-8句
4. `## 词汇表`
   - 精选高频核心词（解析中已详细讲解的词汇可省略）
   - **重音标注**：词汇表中的词标注重音位置（如 `spavénto`、`belva`）
   - 格式：词汇/短语 | 释义 | 重音 | 难度（A1-C1）
5. `## 固定表达`
   - 精选故事中的idiomi、 proverbi、sette espressioni
   - 格式：表达 | 释义 | 例句
6. `## 文化注释`
   - 故事涉及的历史/政治/地理/社会背景
   - 帮助理解意大利语内容的文化语境
7. `## 文学手法`
   - **叙事视角**：
   - **意象/象征**：
   - **语言特点**：
8. `## 主题探讨`
   - **核心主题**：
   - **与作者其他作品的联系**（如适用）：
   - **与当代的关联**（可选）：
9. `## 精读笔记`
   - **中意对比难点**：中国人学意大利语的特殊注意点（如虚拟式、动词时态选择、前置词难点）
   - **骨架句型**：可迁移的句型骨架，替换词汇即可用于不同场景
   - **可迁移表达**
   - **难句回顾**
   - **思考问题**（供讨论/写作用）
   - **与作者其他作品的联系**（如适用）：
   - **与当代的关联**（可选）：
7. `## 精读笔记`
   - **可迁移表达**
   - **难句回顾**
   - **思考问题**（供讨论/写作用）

### 故事精读的核心原则

- **原文分段呈现**：故事全文分段，每段配翻译和注释，不再是一大坨
- **词汇从原文中来**：每个词汇注释都指向故事中的具体句子
- **文学分析适度**：B1级别侧重情节理解和表达积累，B2+增加文学手法分析
- **动词变位重点标注**：仅针对对话中的关键句和复杂描写句，非每句必标
- **结尾无 ■ 标记**

## 会话交互指令
- "继续" = 接着分析下一部分
- "详细解释这个句子" = 只深挖该句
- "只讲语法"/"只讲词汇" = 只分析该维度
- "测试我" = 设计练习不直接给答案
- "不要翻译" = 全意语用简单意语解释意语
- "标难度" = 标注文中各句难度等级

**核心原则**：会话保持精简，详情写入文件。用户读文件而不是会话。

## 新闻精读工作流

### 源文件管理
- `news/` 存放 Calibre 抓取的 epub 新闻（不 git 追踪）
- `content/<source>/` 存放精读文档

### 抓取工具
1. **Calibre Fetch News**：用户手动抓取 epub 到 `news/`
2. **fetch_ilpost.py**：使用 Il Post API（`pip install ilpost-api-wrapper`）
3. **fetch_trafilatura.py**：通用全文提取，支持任意 URL（`pip install trafilatura`）
4. **fetch_doppiozero.py**：doppiozero RSS（唯一有全文的 RSS 源）

### 处理流程
1. 用户用 Calibre 抓取新闻 → epub 保存到 `news/`
2. 用 ebook-convert 提取文本：`ebook-convert "news/xxx.epub" /tmp/xxx.txt`
3. 或使用 `fetch_ilpost.py` 直接获取 Il Post 文章
4. 从文本中选择适合精读的文章（800-1000词核心段落，B1-C1难度）
5. 生成精读文档 → 保存到 `content/<source>/<日期_星期>/`

### 文章选择标准
- 词数：800-1000词（核心段落）
- 难度：B1-C1
- 主题：文化、社会、生活、科技（避免纯政治/体育）
- 语言：清晰规范，有学习价值
