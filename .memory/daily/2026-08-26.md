# 2026-08-26

## Quartz 文档排序机制（调研+修复）

### 问题
ItalianRead 列表页 `racconti/` 下的故事看似"随机乱序"——01 Novellino 出现在末尾、修复轮文件插入其他位置。

### 排查链
1. **PageList.tsx** 默认排序 `byDateAndAlphabeticalFolderFirst`：文件夹优先 → 双方有日期则按 modified 倒序 → 无日期则标题字母序
2. **explorer 侧边栏**：localeCompare(numeric: true)，文件名编号起作用；这层始终是 01→N 正序
3. **created-modified-date 插件**：只读 frontmatter `created/modified/published`，**不读 `date:`**——之前以为改了 date 影响排序是错觉
4. ItalianRead 故事的 `date:` 字段是批量生成时刻（5分钟递增），与文件名编号无关

### 修复方案（用户审批）
**每文件夹一个统一 modified = 该目录精读启动日**，触发稳定排序 → 输入序 = 文件名序 = 01→N 正序。

| 文件夹 | modified (git log 推断首篇精读日) | 列表结果 |
| ------ | ----------------------------------- | -------- |
| first_italian_reader | Fri, 22 Aug 2026 | 01→55 ✅ |
| penguin_parallel | Fri, 22 Aug 2026 | 01→07 ✅ |
| italian_reader_en_it | Fri, 22 Aug 2026 | 01→13 ✅ |
| touri | Fri, 22 Aug 2026 | 01→10 ✅ |
| olly_richards | Fri, 22 Aug 2026 | 01→08 ✅ |
| first_italian_readings | Fri, 22 Aug 2026 | 01→15 ✅ |
| italian_short_stories_100 | Fri, 22 Aug 2026 | 001→097 ✅ |

- Commit `4ec4c96`：每文件统一"Fri, 22 Aug 2026"（粗暴方案）
- Commit `55d7207`：试图每文件夹不同（Aug22/23/26）—— 用户反馈 touri/first_italian_readings 的 Aug26 错（实际是脚本写入 mtime 而非项目启动）
- Commit `21a48a6`：最终版——所有7目录都用真实启动日 2026-08-22

## md2web 标准工作流（已建独立仓库）

- 位置：`/Users/jcxs2014/Sites/OpenCodeFiles/md2web/`（独立 git 仓库，与 multi-ide-template 平级）
- OpenCodeFiles 根 .gitignore 已加 `md2web/`
- 内容：WORKFLOW.md（四层：L1流程/L2清单/L3坑位/L4案例）+ template/ 模板包 + examples/ 两站案例 + scripts/check_site.sh 体检脚本 + scripts/CHECK_SITE.md
- 审查发现 setup_quartz.sh 第70行 `sed -i` 在 macOS（BSD sed）上需要 `-i ''`，建议对方修正

## 记忆库重构（对齐 EnglishRead）

按用户反馈 ItalianRead 的 `.memory/` 不该用单文件 `ItalianRead_MEMORY.md`，应按 EnglishRead 三层结构：
- `.memory/AGENTS.md`（长期项目记忆 + 协作协议）
- `.memory/daily/YYYY-MM-DD.md`（每日日志，高频变动）
- `COLLABORATION.md`（跨机消息板）

重构 commit `98ae6df`：
- `ItalianRead_MEMORY.md` → `AGENTS.md`（去除 253 篇统计等高频数据）
- 新建 `.memory/daily/2026-08-25.md`（承接每日工作日志）

## 身份修正

`COLLABORATION.md` 中的发送方从 `ItalianRead-IDE` 改为 `Opencode-MAC`（用户明确身份），历史条目保留原标注。Commit `664bcca`。

## 字体栈镜像待办

ItalianRead 的 custom.scss 仍是元素级硬编码 `font-family !important`（article/h1/code），未镜像 EnglishRead 的"变量层"统一方案：
- config typography 已修（header: Lora → 后续若要中文宋体需 header: Noto Serif SC）
- custom.scss 待简化为只改 `--headerFont/--bodyFont/--codeFont` 变量层
- 等用户推进

## 双 AGENTS.md 重构 + .memory 入库

### 背景
用户参照 EnglishRead 的两个 AGENTS.md 分工策略，要求 ItalianRead 也做同样重构。EnglishRead 的分工：根 AGENTS.md = 执行规则（入 git），.memory/AGENTS.md = 协作基础设施（不入 git）。

### 调研结论
EnglishRead 根 AGENTS.md 包含：工作流/来源/命名格式/git 策略/推送/Quartz 红线/MiniMax 1027/跨 IDE 协作指针。ItalianRead 根 AGENTS.md 当时只有：精读流程/格式/交互指令/MiniMax（无此配置）。缺失大量执行规则。

### 重构内容
1. **根 AGENTS.md（入 git）**：补齐来源清单、日期文件夹规范、git 与推送策略、Quartz 配置红线（排序/typography/前端定制/PWA/红线文件/嵌套副本），整合精读格式要点
2. **.memory/AGENTS.md（入 git）**：精简为协作基础设施（跨 IDE 约定/三层分工/Quartz 活链结构/系统设计决策），移除与根重复内容
3. **互引指针**：根末尾加「见 .memory/AGENTS.md」，.memory 头部加「见根 AGENTS.md」
4. **README**：目录树注释加「入 git」语义，新增记忆与协作分工四层表格

### 关键原则
- 根 = 执行规则（agent 接到精读任务时必读，入 git 同步所有 IDE）
- .memory = 协作基础设施（跨 IDE 拓扑，入 git 同步）
- 不重复：根是精读执行时的唯一真实源
- 不遗漏：构建约束/命名规范/Quartz 红线必须在根

### Commits
- `9868c33`：重构双 AGENTS.md 分工 + README 记忆分层说明
- `5da3338`：.memory 入库后同步相关说明（入 git 标注统一）

### 已知待办
- custom.scss 字体变量层镜像（EnglishRead commit 75f31cd 已做，ItalianRead 待执行）