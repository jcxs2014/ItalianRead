# ItalianRead — Git Ignore

## CortexKit / OpenChamber 临时文件
.cortexkit/
.openchamber/

## 故事书源文件（epub，不入git）
storybook/

## Quartz 静态站点生成
/quartz/public/          # Quartz 输出目录（CF Pages 部署，每次构建覆盖）
/quartz/.quartz/        # Quartz 构建缓存
/quartz/node_modules/    # Quartz 依赖（npm install）
*.lock

## 思源笔记数据库
*.db
*.db-shm
*.db-wal

## macOS
.DS_Store
.AppleDouble
.LSOverride

## 临时文件
*.swp
*.swo
*~
.#*

## 抓取缓存
*.cache/
__pycache__/
*.pyc

## Obsidian 配置（用户自行管理）
.obsidian/

## Python 虚拟环境
venv/
.venv/
env/

## Cloudflare Pages 部署相关
# CF Pages 构建命令：npx quartz build --directory ../content/articoli
# CF Pages 输出目录：public
# 环境变量：NODE_VERSION=22
#
# 注意：quartz/public/ 不进 git（由 quartz build 自动生成）
